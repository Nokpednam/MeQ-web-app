import {redirect}from "next/navigation";import{SupabaseCourtsView}from "@/components/supabase-court-views";import{createClient}from "@/lib/supabase/server";import{getSupabaseQueueData}from "@/lib/supabase-queue-repository";export default async function CourtsPage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/courts");return <SupabaseCourtsView data={await getSupabaseQueueData(s,user.id)}/>}
